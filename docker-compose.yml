services:
    flask_backend:
      container_name: flask
      build: 
        context: .
        dockerfile: Dockerfile
      ports:
        - "5000:5000"
      
      volumes:
        - .:/app
      restart: always